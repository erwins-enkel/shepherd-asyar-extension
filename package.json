{
  "name": "blog.osthoff.shepherd",
  "version": "0.1.0",
  "description": "Open Shepherd sessions and see which ones are waiting on you.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build",
    "check": "tsc --noEmit",
    "validate": "asyar validate"
  },
  "dependencies": {
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "asyar-sdk": "^4.6.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0"
  }
}
