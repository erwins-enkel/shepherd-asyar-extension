{
  "name": "dev.erwins-enkel.shepherd",
  "version": "0.1.0",
  "description": "See which of your Shepherd coding-agent sessions are waiting on you and open one in a keystroke. Requires a self-hosted Shepherd core; read-only.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "vite build",
    "check": "tsc --noEmit",
    "validate": "asyar validate",
    "test": "vitest run"
  },
  "dependencies": {
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@types/node": "^22.0.0",
    "asyar-sdk": "^4.6.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0",
    "vitest": "3.2.4"
  }
}
